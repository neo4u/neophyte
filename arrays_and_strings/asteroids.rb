# @param {Integer[]} asteroids
# @return {Integer[]}
def asteroid_collision(asteroids)
  prev, n = asteroids.first, asteroids.size
  result = []
  1.upto(n - 1) do |i|
    re
  end
end

def collide(a, b)
  return [] if a == b
  return a.abs > b.abs ? [a] : [b]
end

def sign(num)
  num <=> 0
end

# 735. Asteroid Collision
